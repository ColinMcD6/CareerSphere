const enum verificationType {
    emailVerification = "email_verify",
    passwordReset = "reset_pass",
}
// enum used to inject these values everywhere than just the object.

export default verificationType;
