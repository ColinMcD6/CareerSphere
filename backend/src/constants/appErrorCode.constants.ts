const enum AppErrorCode {
    InvalidToken = "InvalidAccess",
}

// This enum is used to define the error codes for the application. It is used in the error handling middleware to send the appropriate error response to the client.
export default AppErrorCode;
